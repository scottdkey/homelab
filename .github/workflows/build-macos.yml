name: Build and Release macOS

on:
  workflow_dispatch:
    inputs:
      build:
        description: "Build macOS CLI"
        required: false
        type: boolean
        default: true
      release:
        description: "Release macOS CLI (requires build first)"
        required: false
        type: boolean
        default: false
      version:
        description: "Version to release (defaults to Cargo.toml version)"
        required: false
        type: string

permissions:
  contents: read
  actions: write

env:
  RUST_VERSION: stable

jobs:
  approval-to-build:
    name: Approval to Build
    runs-on: ubuntu-latest
    if: github.event.inputs.build == true
    environment:
      name: ${{ github.event.inputs.to_branch }}-approval
    steps:
      - name: Request approval to build
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "  Approval Required: Build macOS CLI"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "Approving this will build the macOS CLI for all supported targets."
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

  build:
    name: Build macOS CLI
    runs-on: macos-14
    needs: approval-to-build
    if: needs.approval-to-build.result == 'success'
    strategy:
      matrix:
        target:
          - x86_64-apple-darwin
          - aarch64-apple-darwin
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache Cargo dependencies
        uses: actions/cache@v4
        with:
          path: |
            ${{ runner.tool_cache }}/cargo
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-${{ matrix.target }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-${{ matrix.target }}-
            ${{ runner.os }}-cargo-

      - name: Build Rust CLI
        run: |
          unset MACOSX_DEPLOYMENT_TARGET || true
          cargo build --release --target ${{ matrix.target }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: hal-${{ matrix.target }}
          path: target/${{ matrix.target }}/release/hal
          retention-days: 90
          if-no-files-found: error

  approval-to-release:
    name: Approval to Release
    runs-on: ubuntu-latest
    needs: [approval-to-build, build]
    if: |
      always() &&
      needs.approval-to-build.result == 'success' &&
      github.event.inputs.release == true &&
      (needs.build.result == 'success' || needs.build.result == 'skipped')
    environment:
      name: ${{ github.event.inputs.to_branch }}-approval
    steps:
      - name: Request approval to release
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "  Approval Required: Release macOS CLI"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "Build completed successfully."
          echo ""
          echo "Approving this will:"
          echo "  - Create a GitHub release"
          echo "  - Upload macOS build artifacts"
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

  release:
    name: Release macOS CLI
    runs-on: ubuntu-latest
    needs: [approval-to-build, build, approval-to-release]
    if: |
      needs.approval-to-build.result == 'success' &&
      needs.approval-to-release.result == 'success' &&
      (needs.build.result == 'success' || needs.build.result == 'skipped')
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Get version
        id: version
        run: |
          if [ -n "${{ github.event.inputs.version }}" ]; then
            VERSION="${{ github.event.inputs.version }}"
          else
            VERSION=$(grep '^version = ' Cargo.toml | sed 's/version = "\(.*\)"/\1/')
          fi
          # Remove 'v' prefix if present
          VERSION="${VERSION#v}"
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Release version: $VERSION"

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Prepare release archives
        run: |
          mkdir -p release
          VERSION="${{ steps.version.outputs.version }}"

          for artifact_dir in artifacts/hal-*/; do
            if [ -d "$artifact_dir" ]; then
              TARGET=$(basename "$artifact_dir" | sed 's/hal-//')
              echo "Processing: $TARGET"
              
              # Extract architecture from target
              ARCH_RAW=$(echo "$TARGET" | cut -d'-' -f1)
              
              # Convert architecture names
              case "$ARCH_RAW" in
                x86_64)
                  ARCH="amd64"
                  ;;
                aarch64)
                  ARCH="arm64"
                  ;;
                *)
                  ARCH="$ARCH_RAW"
                  ;;
              esac
              
              if [ -f "$artifact_dir/hal" ]; then
                BINARY_NAME="hal"
                OS="darwin"
                
                mkdir -p "release/${OS}-${ARCH}"
                cp "$artifact_dir/$BINARY_NAME" "release/${OS}-${ARCH}/$BINARY_NAME"
                chmod +x "release/${OS}-${ARCH}/$BINARY_NAME"
                
                ARCHIVE_NAME="hal-${VERSION}-${OS}-${ARCH}.tar.gz"
                tar -czf "$ARCHIVE_NAME" -C "release/${OS}-${ARCH}" "$BINARY_NAME"
                echo "Created: $ARCHIVE_NAME"
              fi
            fi
          done

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ steps.version.outputs.version }}
          name: Release v${{ steps.version.outputs.version }} (macOS)
          body: |
            ## macOS Release v${{ steps.version.outputs.version }}

            ### Downloads
            - See assets below for macOS-specific binaries

            ---
            *This release was created automatically by the macOS build workflow.*
          files: |
            hal-*-darwin-*.tar.gz
          draft: false
          prerelease: false
